# benchmark_config.py


# This file serves as the config file for computing time and memory overhead of romp
# The benchmark_parameter_map maps program name to its parameter

benchmark_parameter_map = {
 "c_loopA.badSolution.par" : "-test",
}

